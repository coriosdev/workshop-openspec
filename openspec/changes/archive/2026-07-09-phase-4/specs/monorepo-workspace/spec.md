## MODIFIED Requirements

### Requirement: README documents bootstrap

The repository SHALL include a `README.md` with prerequisites, install, database setup, seed, and dev instructions.

#### Scenario: New developer can follow README

- **WHEN** a developer clones the repo and follows the README
- **THEN** they can run `pnpm install`, set up the database, seed data, and `pnpm dev` successfully
